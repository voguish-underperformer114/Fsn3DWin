# Screenshots

Public screenshots for the GitHub README live here.

Use only synthetic demo scenes or sanitized folders such as `C:\Fsn3dWin\Sample Gallery` for captured scans. Do not capture private user folders, personal file names, desktop contents, or work directories.

Runtime screenshots created by `F12` are written to the root-level `screenshots/` folder, which is intentionally ignored by git.
